# Affinity Test

Static HTML, no build step, no dependencies.

| File | What it is |
| --- | --- |
| `affinity-ops-deck.html` | Ops deck — prediction desk, comms queue, research library and a shipping equities basket. Single file. |
| `Macro_Topics_2026_05_18.html` | Macro Topics reader. |
| `macro-topics-site/` | The reader packaged for deployment (GitHub Pages / Vercel). |

Open any of them straight in a browser, or serve the folder with `npx serve .`.

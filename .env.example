# Copy to .env for local work. .env is gitignored.
#
# The two PUBLIC_SUPABASE_ values have to be set in the Vercel project as well:
# report pages are prerendered, so the BUILD reads content, not just the runtime.
# Production is the `hms-vanguard` project.

# OPTIONAL. On Vercel the site URL comes from VERCEL_PROJECT_PRODUCTION_URL,
# which the platform sets to the building project's own production domain — so
# production is self-configuring and previews canonicalise to the real site
# rather than to themselves.
#
# Set this only to override that, and there is one case where you must: if a
# second Vercel project is also building this repo, each one canonicalises to
# itself and search engines see two copies of the same site. Pin both to the
# production domain, or stop the second project building.
# PUBLIC_SITE_URL=https://hms-vanguard.vercel.app

# Project: affinity-research (Dorsal org, eu-west-2 London)
PUBLIC_SUPABASE_URL=https://zwoaopkxjyydjlakrqwx.supabase.co

# Publishable key. Public by design — it ships inside the page, and what
# protects the data is RLS rather than this string. Through it a caller can read
# published reports and the data behind them and nothing else; the funnel tables
# have RLS with no policy, so they are unreachable with it.
PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_HnHSRM9eROGQJpCVE3jesQ_XP2kQqtp

# Service role. BYPASSES RLS COMPLETELY. Server only — never in a client script,
# never in a PUBLIC_ variable, never committed. Take it from
# Supabase → Project Settings → API Keys → service_role.
# Not needed until Phase 3 (the admin) and Phase 4 (the analytics endpoint).
SUPABASE_SERVICE_ROLE_KEY=

# OPTIONAL. Which content source to render from, when the automatic choice is
# wrong for where you are.
#
#   (unset)   credentials present -> the database; absent -> the committed seed.
#   seed      render src/content/ even though credentials are present. This is
#             what you want in a sandbox with no egress to the Supabase host:
#             the build would otherwise reach for the database and fail, which
#             is correct behaviour and unhelpful locally.
#   database  require the database, and fail rather than fall back.
#
# There is deliberately NO silent fallback. A build that quietly served
# placeholder copy while believing it was live is the worst outcome available
# here, and a free-tier project pauses after about a week idle — which under a
# fallback would replace live content with the seed on the next deploy.
# CONTENT_SOURCE=seed

# OPTIONAL. The Affinity research portal the funnel points at. Until it is set,
# every report's closing CTA says the link is not configured rather than
# linking somewhere invented — see src/lib/config.ts.
# PUBLIC_PORTAL_URL=https://research.example.com

# OPTIONAL, server only. A Vercel deploy hook, so publishing a report rebuilds
# the site. Report pages are prerendered, so a publish is invisible to readers
# until a build runs — this is what makes the publish button mean it.
# Without it the admin says so rather than pretending the report is live.
# Vercel -> Project -> Settings -> Git -> Deploy Hooks.
# Secret: anyone holding the URL can trigger builds on this project.
# VERCEL_DEPLOY_HOOK_URL=
